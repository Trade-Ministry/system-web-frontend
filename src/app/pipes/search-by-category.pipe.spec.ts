import { SearchByCategoryPipe } from './search-by-category.pipe';

describe('SearchByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
